{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvx mcp-server-motherduck --transport stream --port ${PORT:-8080} --db-path :memory:",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
